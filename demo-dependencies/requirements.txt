requests==2.23.0
boto3

# Using the bellow command, can install the all dependencies listed in this file
# pip install -r requirements.txt   
# pip install virtualenv            # First, have to install virtualenv from that commnd.
# virtualenv test_env               # This will create a testing environment.
# source test_env/bin/activate      # Then you have to activate the virtual environment.
                                    # Once you executing the pip install commands, it will install the packages inside the virtual environment.
                                    # By running "deactivate", can deactivate the virtual environment.
