import frontend.front as front  # Import from frontend directory front module.
import backend.back as back     # Import from backend directoty back module.

print(front.front_name())
print(back.back_name())