## Importing the custom module ##
import demo_module_0                    # Referencing this module, its the name of the Python file that contains the required function 

print(demo_module_0.function_name())    # When calling the function 1st specify the module name and then the function name
